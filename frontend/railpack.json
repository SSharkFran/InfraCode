{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "[ -f package.json ] && npm install --include=dev --no-audit --no-fund || (cd frontend && npm install --include=dev --no-audit --no-fund)"
      ]
    },
    "build": {
      "commands": [
        "[ -f package.json ] && npm run build || (cd frontend && npm run build)"
      ]
    }
  },
  "deploy": {
    "startCommand": "[ -f package.json ] && npm run start || (cd frontend && npm run start)"
  }
}
