{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "inputs": ["."],
      "commands": [
        "if [ -f frontend/package.json ]; then cd frontend; fi; npm install --include=dev --no-audit --no-fund"
      ]
    },
    "build": {
      "commands": [
        "if [ -f frontend/package.json ]; then cd frontend; fi; npm run build"
      ]
    }
  },
  "deploy": {
    "startCommand": "if [ -f frontend/package.json ]; then cd frontend; fi; npm run start"
  }
}
